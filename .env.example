# Runtime environment (development / production)
NODE_ENV=development

# Database connection string
# Development (SQLite): file:./dev.db
# Production (PostgreSQL): postgresql://user:password@localhost:5432/mydb
# Docker with PostgreSQL: postgresql://remix:password@postgres:5432/remix_dashboard
DATABASE_URL=file:./dev.db

# Session encryption secret key
# Generate with: openssl rand -hex 32
# Or use: https://ipecho.io/tools/password-generator
SESSION_SECRET=your-random-secret-key-here

# Optional: Skip automatic database initialization (default: false)
# SKIP_DB_INIT=true

# Optional: Skip seed data insertion (default: false, recommended to set true in production)
# SKIP_SEED=true
